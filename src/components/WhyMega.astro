---
const points = [
  {
    title: "Forward-deployed, not hands-off",
    description:
      "We embed directly with your team through design, build, and launch — accountable to shipped systems, not a slide deck.",
  },
  {
    title: "AI-native architecture",
    description:
      "Every system we design assumes AI/ML is core to the product, not bolted on after the fact.",
  },
  {
    title: "Multi-cloud, by design",
    description:
      "Hands-on expertise across both AWS and Google Cloud — compute, storage, data, and AI/ML services — so the recommendation fits the workload, not a single vendor.",
  },
  {
    title: "Industry-specific",
    description:
      "We only work in media, entertainment, gaming, and AI — so recommendations come from pattern-matching real workloads, not generic best practices.",
  },
];
---

<section class="why" id="why">
  <div class="container why-grid">
    <div class="head">
      <span class="eyebrow" style="color:#7fd4ee">Why MEGA Cloud Consulting</span>
      <h2>Built by engineers who ship, not just advise.</h2>
      <p>
        We're a small team of forward-deployed builders who specialize in
        media, entertainment, gaming, and AI — and who'd rather be in your
        codebase than in a slide deck.
      </p>
    </div>

    <div class="points">
      {
        points.map((point) => (
          <div class="point">
            <h3>{point.title}</h3>
            <p>{point.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .why {
    padding: 5rem 0;
    background: var(--navy);
    color: #fff;
  }

  .why-grid {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: 3rem;
  }

  .head h2 {
    color: #fff;
    font-size: clamp(1.6rem, 3vw, 2.1rem);
    margin-bottom: 1rem;
  }

  .head p {
    color: rgba(255, 255, 255, 0.72);
    max-width: 38ch;
    font-size: 1rem;
  }

  .points {
    display: grid;
    gap: 1.75rem;
  }

  .point {
    padding-left: 1.25rem;
    border-left: 2px solid rgba(255, 255, 255, 0.18);
  }

  .point h3 {
    color: #fff;
    font-size: 1.05rem;
    margin-bottom: 0.4rem;
  }

  .point p {
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.94rem;
  }

  @media (max-width: 860px) {
    .why-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .head p {
      max-width: none;
    }
  }
</style>
